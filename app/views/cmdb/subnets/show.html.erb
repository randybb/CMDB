<h1>CMDB: Subnet <%= @subnet[:cmdb][:name] %></h1>

<table class="table table-striped table-condensed">
  <tbody>
<% @subnet[:cmdb].each do |key, value| %>
    <tr>
      <% ( %><th><%= key %></th><td><%= value %></td><% )  %>
    </tr>
<% end %>
  </tbody>
</table>
