<h1>CMDB: Interface <%= @interface[:cmdb][:name] %> on <%= @interface[:cmdb][:deviceid_value] %></h1>

<table class="table table-striped table-condensed">
  <tbody>
<% @interface[:cmdb].each do |key, value| %>
    <tr>
      <% ( %><th><%= key %></th><td><%= value %></td><% )  %>
    </tr>
<% end %>
  </tbody>
</table>
